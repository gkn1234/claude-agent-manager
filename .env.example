# Database
DB_PATH=./data/dispatch.db

# Logs
LOG_DIR=./logs

# Server
PORT=3000

# Scheduler defaults (also configurable via Settings UI)
MAX_CONCURRENT=2
COMMAND_TIMEOUT=1800
POLL_INTERVAL=5

# Auth
AUTH_PASSWORD=changeme
AUTH_SECRET=replace-with-random-string
